name: user_repository
description: A repo to manage users of unimaps
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.7.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  firebase_auth: ^5.5.1
  cloud_firestore: ^5.6.5
  equatable: ^2.0.7
  google_maps_flutter: ^2.11.0


dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true  
